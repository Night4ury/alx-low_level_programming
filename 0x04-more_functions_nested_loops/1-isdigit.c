#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isdigit - Function Title
 *
 * Description: Loading...
 *
 * @c: number
 *
 * Return: nothing
 */

int _isdigit(int c)
{
if (isdigit(c))
{
return (1);
}
else
{
return (0);
}
}
